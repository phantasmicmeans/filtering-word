spring:
  servlet:
    multipart:
      max-file-size: 1MB
      max-request-size: 1MB

  mvc:
    contentnegotiation:
      favor-parameter: true
      favor-path-extension: true

redis:
  host: localhost
  port: 6379
  password:

server:
  port: 8081